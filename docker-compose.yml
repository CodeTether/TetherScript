services:
  kiln-landing:
    build: .
    image: kiln:latest
    container_name: kiln-landing
    ports:
      - "8787:8787"
    restart: unless-stopped
    # Swap in any example by overriding the command:
    #   command: ["/examples/http_hello.tether"]
