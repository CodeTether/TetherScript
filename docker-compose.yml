services:
  tetherscript-landing:
    build: .
    image: tetherscript:latest
    container_name: tetherscript-landing
    ports:
      - "8787:8787"
    restart: unless-stopped
    # Swap in any example by overriding the command:
    #   command: ["/examples/http_hello.tether"]
