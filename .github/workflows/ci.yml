name: CI

on:
  pull_request:
  push:
    branches:
      - main
  # Allows a maintainer to run the gates on demand, e.g. when a push made with a
  # bot token does not spawn an automatic run.
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt

      - run: bash ./check_file_limits.sh
      - run: cargo fmt --check
      - run: cargo clippy -- -D warnings
      - run: cargo test --test browser_wpt_like
      - run: cargo test --test browser_wpt_json
      - run: cargo test --test browser_wpt_upstream
      - run: cargo test
      - run: cargo test --doc
      - run: RUSTDOCFLAGS="-D warnings" cargo doc --no-deps
      - run: cargo package
