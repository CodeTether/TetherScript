/target
/examples/actix-web-demo/target/
/AGENT.md
/.claude
/.codetether-agent/
/leftoff.md
/prd_*.json
/us_001/

# vscode extension build artifacts
editor/vscode/node_modules/
editor/vscode/*.vsix
editor/vscode/package-lock.json
.codetether-worktrees/
.codetether/

# TetherScript local agent runtime state
.tetherscript/
examples/.tetherscript/

# Local validation and benchmark artifacts
/.tmp/
/benchmark-results.json

# Local HTTPS identities (never commit private keys)
/certs/
# Client-specific application ports are developed against private schemas and credentials, so
# they live outside this repository. Keep the pattern here broad rather than naming a customer.
/examples/client-*
/tests/client_*
*_port_private*
.tmp/
