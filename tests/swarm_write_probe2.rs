//! Swarm write probe: verifies the harness can create an integration test file.

/// Asserts basic arithmetic to prove the probe file compiles and runs.
#[test]
fn probe2() {
    assert_eq!(2 + 2, 4);
}
